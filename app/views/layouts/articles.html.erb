<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    'articles', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= render 'application/favicon' %>
  <!--[if lt IE 9]>
    <script src-"cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
    </script>
    <![endif]-->
</head>
<body>
  <header id="navbar-header">
      <div class="navbar navbar-fixed-top navbar-inverse">
        <div class="container-fluid">
          <div id="header-logo">
            Glenn's Music Blog
          </div>
          <div class="navbar-brand">
          <%= Time.now.strftime("%B %d, %Y") %>
          </div>
          <nav>
            <ul class="nav navbar-nav navbar-right">
              <li><%= link_to "Back to Site", root_path %></li>
              <% if logged_in? %>
              <li><%= link_to "Log out", logout_path, method: "delete" %></li>
              <% end %>
              <% if !logged_in? %>
              <li><%= link_to "&#9776".html_safe, "javascript:openNav()", id: "blog-nav-hamburger" %></li>
              <% end %>
            </ul>
          </nav>
        </div>
      </div>

      <div class="sidenav" id="blog-sidenav">
        <h3 class="text-center">Admin</h3>
        <%= link_to "&times;".html_safe, "javascript:closeNav()", class: "closebtn" %>
        <%= link_to "Log in", login_path %>
      </div>

  </header>

  <div class="container-fluid article-page">
    <%= yield %>
  </div>
</body>
</html>
